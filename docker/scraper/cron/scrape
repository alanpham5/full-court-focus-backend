SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin
APP_DIR=/app

# Daily check at 6:05 UTC; script runs scrape only if stale vs Nov–Apr schedule.
5 6 * * * root /usr/local/bin/run-scrape.sh >> /var/log/scrape.log 2>&1
