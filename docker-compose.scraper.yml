services:
  scraper:
    build:
      context: .
      dockerfile: docker/scraper/Dockerfile
    restart: unless-stopped
    env_file:
      - app/.env
    environment:
      APP_DIR: /workspace/app
      REPO_ROOT: /workspace
    volumes:
      - .:/workspace
